<!DOCTYPE html>
<html>
  <head>
    <title>Pw</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar navbar-dark bg-dark">
      <div class="container">
        <%= link_to "PW", root_path, class: 'navbar-brand' %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <%= link_to 'Clients', clients_path, class: 'nav-link' %>
            </li>
            <li class="nav-item">
              <%= link_to 'Buildings', buildings_path, class: 'nav-link' %>
            </li>
          </ul>
        </div>
        <% if current_client %>
          <%= link_to "#{current_client.name}", current_client, class: 'navbar-text' %>
        <% else %>
          <%= link_to "Login", login_path, class: 'navbar-text' %>
        <% end %>
      </div>
    </nav>
    <div class="container mt-3">
      <%= yield %>
    </div>
  </body>
</html>
